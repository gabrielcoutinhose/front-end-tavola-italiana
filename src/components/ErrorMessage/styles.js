import styled from "styled-components";

export const StyledErrorMessage = styled.p`
  font-style: normal;
  font-weight: normal;
  font-size: 14px;
  line-height: 16px;
  color: var(--problem-color);
  margin-top: 2px;
`;
